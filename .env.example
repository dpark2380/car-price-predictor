# Marketcheck API (required for ingestion — get a key at marketcheck.com)
MARKETCHECK_API_KEY=
MARKETCHECK_ZIP=
MARKETCHECK_RADIUS=

# Flask API
PORT=

# Scheduler / training tuning (optional, sane defaults in code)
INACTIVE_AFTER_DAYS=
MIN_TRAINING_SAMPLES=
STALE_LISTING_MAX_DAYS=
